<nav class="navbar">
  {{#link-to "channels.all"}}
    <div class="logo">
      bump
    </div>
  {{/link-to}}
  {{#if session.isAuthenticated}}
    <button {{action 'logout'}} class="signin-link">Logout</button>
  {{else}}
    {{#link-to "signin"}}
      <div class="signin-link">
        login
      </div>
    {{/link-to}}
  {{/if}}
</nav>
<div class="app-container">
  {{outlet}}
</div>
